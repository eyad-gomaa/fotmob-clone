import 'package:flutter/material.dart';

class ReviewLeaguesViewBody extends StatelessWidget {
  const ReviewLeaguesViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}