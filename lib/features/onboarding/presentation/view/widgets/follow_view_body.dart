import 'package:flutter/material.dart';

class FollowViewBody extends StatelessWidget {
  const FollowViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
